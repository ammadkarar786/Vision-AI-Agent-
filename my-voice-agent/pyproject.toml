[project]
name = "my-voice-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ammad Sohail Karar", email = "56234762+ammadkarar786@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "vision-agents>=0.6.9",
]

[project.scripts]
my-voice-agent = "my_voice_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
